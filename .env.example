# ──────────────────────────────────────────────
# TraudeZ99-3 Trading Bot — Environment Config
# ──────────────────────────────────────────────
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit your .env file to git.
# ──────────────────────────────────────────────

# General
NODE_ENV=development
LOG_LEVEL=info                    # fatal | error | warn | info | debug | trace

# Exchange API Keys
# Get these from your exchange account settings.
# Use READ-ONLY keys for paper trading.
# NEVER enable withdrawal permissions.
BINANCE_API_KEY=
BINANCE_API_SECRET=
KRAKEN_API_KEY=
KRAKEN_API_SECRET=
COINBASE_API_KEY=
COINBASE_API_SECRET=

# Database
DATABASE_URL=file:./dev.db        # SQLite for development
# DATABASE_URL=postgresql://user:password@localhost:5432/tradez99  # PostgreSQL for production

# Trading Configuration
TRADING_MODE=paper                # paper | live  (ALWAYS start with paper)
DEFAULT_EXCHANGE=binance          # binance | kraken | coinbase
TRADING_PAIRS=BTC/USDT,ETH/USDT  # Comma-separated trading pairs
DEFAULT_TIMEFRAME=1h              # Candle timeframe: 1m, 5m, 15m, 1h, 4h, 1d

# Strategy
DEFAULT_STRATEGY=sma-cross        # Strategy to run

# Risk Management
MAX_POSITION_SIZE_PCT=5           # Max % of portfolio per position
DAILY_LOSS_LIMIT_PCT=3            # Halt trading if daily loss exceeds this %
MAX_OPEN_POSITIONS=3              # Max concurrent open positions
COOLDOWN_LOSSES=3                 # Consecutive losses before cooldown
COOLDOWN_DURATION_MIN=60          # Cooldown duration in minutes

# Alerts — Telegram
TELEGRAM_BOT_TOKEN=               # Get from @BotFather on Telegram
TELEGRAM_CHAT_ID=                 # Get from @userinfobot on Telegram

# Alerts — Discord (optional)
DISCORD_WEBHOOK_URL=

# Server (health check & metrics)
HTTP_PORT=3000
